services:
  web:
    image: nginx:1.29-alpine
    ports:
      - "4173:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./fixtures/storage:/usr/share/nginx/storage:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
